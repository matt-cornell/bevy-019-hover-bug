# Bevy hover bug

This is a simple program to demonstrate a bug in Bevy's hover code. To demonstrate, run the app with either `bevy-018` or `bevy-019` features, and hover of the bottom part of the button labeled "First". The partially-clipped button labeled "1" is focused instead with Bevy 0.19.
